---
- name: start or restart bind9 container
  community.docker.docker_compose:
    project_src: "{{ bind9_base_dir }}"
    state: present 

- name: restart systemd
  ansible.builtin.systemd_service:
    state: restarted
    daemon_reload: true
    name: systemd-resolved.service